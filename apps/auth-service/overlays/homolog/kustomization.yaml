apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

images:
  - name: togglemaster/auth-service
    newName: ocir.invalid/togglemaster-phase3/auth-service
    newTag: bootstrap

labels:
  - pairs:
      app.kubernetes.io/environment: homolog
